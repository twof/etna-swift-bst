{
  "setup_steps": [],
  "build_steps": [
    {
      "Command": {
        "command": "${workload_path}/scripts/swift-toolchain.sh",
        "args": ["build"]
      }
    }
  ],
  "capabilities": {
    "solve": [
      {
        "Command": {
          "command": "${workload_path}/scripts/run-bst.sh",
          "args": ["${strategy}", "${property}"],
          "params": ["workload_path", "property", "strategy"]
        }
      }
    ],
    "sample": [
      {
        "Command": {
          "command": "${workload_path}/scripts/run-sampler.sh",
          "args": ["${property}", "${tests}"],
          "params": ["workload_path", "property", "tests"]
        }
      }
    ]
  },
  "tags": {
    "strategy": ["ptk"]
  }
}
